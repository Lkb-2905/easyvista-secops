## Analyseur de headers HTTP de securite

Objectif: verifier la presence de headers OWASP (HSTS, X-Frame-Options, CSP).

Fonctions prevues:
- requete HTTP simple
- extraction des headers
- rapport conforme / non conforme

Livrables:
- script `headers_check.py`
